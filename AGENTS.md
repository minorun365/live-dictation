# このアプリのテストルール

- アプリは無音環境で使う。テスト中に `say` やスピーカー経由の音声再生を行わない。
- テストのためにMacの出力音量を変更しない。
- 音声入力が必要な実機確認は、ユーザーが実際に使う環境で入ったマイク入力だけを使う。
- 画面収録は録音開始と同時にメインディスプレイ全体を自動取得する。録音のたびに画面やウィンドウを選ばせる操作は追加しない。
- 録音フローを変更した場合、ビルド成功と保存処理の単体テストだけで完了扱いにしない。実機で「録音開始→必要なファイルの生成→録音停止」まで通して確認する。
- 上部の録音ステータスは文字だけで表示する。録音中を示す赤ランプや丸いインジケーターは置かない。
- メニューバーに、録音中だと第三者が読み取れる表示を出さない。文字ラベル・ランプ・赤色・バッジは付けない。このアプリは顧客との商談で画面共有しながら使うため、メニューバーに録音の気配が出ると相手に録音が知られてしまう。録音中にアイコンの絵柄を差し替えることだけは行う（`RecordingMenuBarIcon`）。条件は、相手から見て録音の合図と結び付かない絵であること。差し替える絵はユーザーが指定したものだけを使い、こちらで選んだり作ったりしない。
- アプリ名の変更や再パッケージでは、旧インストール版にある既存アイコンなどの視覚資産をそのまま引き継ぐ。新しい画像を生成するのはユーザーが明示した場合だけにする。
- UIや挙動を変更したら、ソースのビルドだけで完了にしない。`scripts/build-app.sh` でRelease版を生成し、`~/Applications/文字起こしちゃん.app` を更新して再起動する。生成版とインストール版の実行ファイルが一致し、インストール版のプロセスが起動したことまで確認する。
- 画面収録などのmacOS権限を更新後も維持するため、`scripts/build-app.sh` の固定Designated Requirementを外さない。インストール後は `codesign -d -r-` の結果が `cdhash` ではなく `identifier "com.minorun365.LiveDictation"` であることを確認する。
